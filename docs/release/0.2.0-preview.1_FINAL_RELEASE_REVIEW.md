# Workflow OS 0.2.0-preview.1 Final Release Review

Review date: 2026-06-04

Reviewed release:

- **Workflow OS 0.2.0-preview.1 — Public Read-Only Integration Preview**
- Release artifacts, live smoke evidence, release process docs, known limitations, user guide, integration docs, security docs, manifests, lockfiles, and CI configuration.

This review does not approve production readiness, write-capable adapters, hosted operation, distributed workers, OAuth, webhooks, generic live adapter execution, production telemetry export, domain packs, or Level 3/4 autonomy.

## 1. Executive Verdict

**Ready for maintainer tag.**

The release pack is accurate, consistent, safe, and ready for a human maintainer to tag after committing the release-pack changes and confirming a clean working tree.

No release-content, versioning, validation, security/privacy, or documentation-honesty blocker remains for the narrow public read-only integration preview posture.

## 2. Release Posture Verification

Verdict: **Pass.**

The public posture is consistently described as:

- public read-only integration preview;
- read-only only;
- opt-in live providers;
- fixture-first normal CI;
- local/kernel-oriented;
- not production-ready;
- not write-capable;
- not hosted;
- not distributed-runtime-ready;
- not Level 3/4 autonomy-ready.

Evidence:

- `README.md` identifies current preview version `0.2.0-preview.1`, explains the `0.1.0-preview.1` local-kernel layer and the `0.2.0-preview.1` read-only integration layer, and denies production, hosted, distributed, write-capable, and Level 3/4 claims.
- `docs/PROJECT_CHARTER.md` describes the two preview layers and explicitly keeps read-only adapters fixture-first in normal CI and opt-in for live providers.
- `docs/release/0.2.0-preview.1_RELEASE_NOTES.md` and `docs/release/0.2.0-preview.1_GITHUB_RELEASE_DRAFT.md` use the approved public read-only integration preview framing.
- `docs/release/V0_KNOWN_LIMITATIONS.md` keeps the v0 limitations and 0.2 accepted limitations visible.

## 3. Release Artifact Verification

Verdict: **Pass.**

| Artifact | Verification |
| --- | --- |
| `CHANGELOG.md` | Includes `0.2.0-preview.1` with read-only GitHub/Jira/GitHub Actions adapters, adapter telemetry mapping, live smoke evidence, limitations, and non-goals. |
| `docs/release/0.2.0-preview.1_RELEASE_NOTES.md` | Accurate release posture, provider scopes, accepted live-smoke limitations, fixture-first CI posture, opt-in live-provider posture, security/privacy notes, telemetry posture, migration note, known limitations, and follow-ups. |
| `docs/release/0.2.0-preview.1_RELEASE_CHECKLIST.md` | Ready for maintainer execution. Includes pre-tag checks, command checks, version checks, docs checks, integration checks, live evidence checks, release-note checks, tag creation, package publication decision, post-release verification, and rollback/unpublish guidance. |
| `docs/release/0.2.0-preview.1_GITHUB_RELEASE_DRAFT.md` | Public-ready and concise. States read-only preview, not production-ready, no writes, opt-in live providers, fixture-first examples, accepted smoke limitations, Jira token rotation recommendation, issue-reporting guidance, and next steps. |
| `README.md` | Current public entry point matches the release posture and gives safe fixture-backed example paths. |
| `ROADMAP.md` | Places Phase 2 read-only preview correctly and keeps writes, production backend, distributed workers, SaaS control plane, UI, marketplace, and high-autonomy writes deferred. |
| `docs/PROJECT_CHARTER.md` | Aligns long-term ambition with current preview boundaries and keeps read-only adapters from defining the product boundary. |
| User-guide docs | Preserve RC1/internal evaluation language while acknowledging the public read-only integration preview and keeping production/writes/high-autonomy boundaries clear. |
| Integration docs | Provider docs describe fixture-first, opt-in live, read-only operation, credential handling, denied writes, and troubleshooting without broad live-provider claims. |

Historical review files intentionally preserve older findings and dates. Their older “pending public preview” language is safe because it is inside historical review artifacts, not current release guidance.

## 4. Version Verification

Verdict: **Pass.**

The version bump has been applied and is not left as a release-operator step.

Evidence:

- `crates/workflow-core/Cargo.toml`: `0.2.0-preview.1`
- `crates/workflow-cli/Cargo.toml`: `0.2.0-preview.1`
- `Cargo.lock`: `workflow-core` and `workflow-cli` are `0.2.0-preview.1`
- root `package.json`: `0.2.0-preview.1`
- `packages/sdk-typescript/package.json`: `0.2.0-preview.1`
- `package-lock.json`: root and SDK package entries are `0.2.0-preview.1`
- `CHANGELOG.md`: includes `0.2.0-preview.1`
- `docs/release/0.2.0-preview.1_RELEASE_NOTES.md`: uses `0.2.0-preview.1`

Command evidence:

- `cargo metadata --locked --format-version 1`: passed.
- `npm pkg get version`: returned `"0.2.0-preview.1"`.
- `npm pkg get version --workspace @workflow-os/sdk-typescript`: returned `{"@workflow-os/sdk-typescript":"0.2.0-preview.1"}`.

CLI version output is represented through package/crate metadata; there is no separately versioned CLI output contract to update.

## 5. Accepted Limitation Verification

Verdict: **Pass.**

Release materials explicitly preserve the required accepted limitations:

- GitHub live smoke exercised `octocat/Hello-World` repository metadata, not `rcs2153/AGT`.
- GitHub Actions live smoke exercised workflow run metadata only.
- Jira live smoke exercised issue metadata only.
- Jira sandbox API token rotation is recommended.
- Adapter telemetry is local/runtime-preview telemetry, not production telemetry export.
- Redaction is deterministic preview redaction, not enterprise DLP.
- No generic live adapter execution from arbitrary workflow specs exists.
- No writes, reruns, dispatches, webhooks, OAuth, hosted operation, production backend, distributed workers, production telemetry export, domain packs, or Level 3/4 autonomy are included.

These limitations appear in the release notes, checklist, GitHub release draft, known limitations, README/charter posture text, and integration readiness rerun.

## 6. Security/Privacy Verification

Verdict: **Pass with required operational follow-up.**

Security/privacy checks:

- No actual token value, private email value, authorization header, or raw provider credential was found in release docs, examples, manifests, lockfiles, or CI configuration.
- Secret search hits were placeholders, auth-mode descriptions, or historical review text.
- `docs/integrations/PHASE_2_LIVE_SMOKE_EVIDENCE.md` does not include token values, authorization headers, raw provider payloads, raw issue bodies, full PR content, raw CI logs, or private credential values.
- Release materials warn not to include secrets in specs, fixtures, examples, docs, screenshots, issues, pull requests, or release evidence.
- Jira sandbox token rotation is called out in the release notes, release checklist, GitHub release draft, readiness rerun, and known limitations.
- Provider metadata sensitivity is disclosed.
- Redaction limitations are disclosed as deterministic preview redaction, not enterprise DLP.

Operational follow-up:

- Rotate the Jira sandbox API token before public promotion. This is not a code/release-pack blocker, but it is a required maintainer hygiene item before announcement.

## 7. Public Overclaim Search

Verdict: **Pass.**

Searches were run for language implying:

- production-ready or enterprise production deployment-ready;
- write support;
- full provider compatibility;
- generic live adapter execution;
- hosted service;
- distributed workers;
- OAuth support;
- webhook ingestion;
- Level 3/4 enabled;
- production telemetry export;
- SIEM/OpenTelemetry integration.

Findings:

- Matches in current public docs are denials, limitations, explicit exclusions, future-work notes, or release-checklist guardrails.
- Matches in historical review docs preserve prior review findings and do not function as current public operator guidance.
- No dangerous false claim was found, and no corrective doc patch was required during this review.

## 8. Required Validation Commands

All commands were run from the repository root. Live tests were not rerun because safe credentials and approved non-sensitive resources were not explicitly requested for this review.

| Command | Result |
| --- | --- |
| `cargo fmt --all --check` | Passed |
| `cargo clippy --workspace --all-targets -- -D warnings` | Passed |
| `cargo test --workspace` | Passed; live GitHub, Jira, and GitHub Actions tests were ignored by default |
| `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` | Passed |
| `cargo audit` | Passed |
| `npm ci` | Passed |
| `npm run check` | Passed |
| `npm run check:contracts` | Passed |
| `npm run check:integrations` | Passed |
| `npm audit --audit-level=moderate` | Passed; `0 vulnerabilities` |
| `cargo metadata --locked --format-version 1` | Passed |
| `npm pkg get version` | Passed; returned `0.2.0-preview.1` |
| `npm pkg get version --workspace @workflow-os/sdk-typescript` | Passed; returned `0.2.0-preview.1` |
| `git diff --check` | Passed |

## 9. Release Checklist Status

Verdict: **Checklist is ready for human maintainer execution.**

The checklist has no content blockers. It correctly requires:

- clean validation commands;
- clean version checks;
- release notes and GitHub draft review;
- live smoke evidence verification;
- Jira sandbox token rotation verification;
- explicit package publication decision;
- human-only tag creation;
- post-release verification;
- rollback/unpublish guidance.

## 10. Package Publication Decision

Verdict: **Manual / not automatic.**

`docs/release/0.2.0-preview.1_RELEASE_CHECKLIST.md` explicitly says package publication is **not automatic**.

For this preview, the default release artifact can be the GitHub tag/release unless maintainers separately decide to publish crates or the TypeScript package. There is no CI workflow that automatically publishes crates or npm packages on tag.

## 11. Tag Readiness

Verdict: **Ready after committing release-pack changes and confirming a clean working tree.**

A human maintainer may proceed with the checklist tag step only after:

1. committing the current release-pack and review changes;
2. running `git status --short`;
3. confirming the working tree is clean.

Then the documented tag commands are appropriate:

```sh
git status --short
git tag -a v0.2.0-preview.1 -m "Workflow OS 0.2.0-preview.1 public read-only integration preview"
git push origin v0.2.0-preview.1
```

Do not create the tag from an uncommitted or dirty tree.

## 12. Blockers

No release blockers remain for `0.2.0-preview.1` as a narrow public read-only integration preview.

## 13. Non-Blocking Follow-Ups

- Rotate the Jira sandbox API token before public promotion.
- Parameterize the GitHub live smoke target so maintainers can exercise `rcs2153/AGT` or another approved repository.
- Add broader opt-in live smoke coverage for GitHub PR/file/comment/check paths.
- Add broader opt-in live smoke coverage for Jira descriptions, comments, and project metadata.
- Add broader opt-in live smoke coverage for GitHub Actions jobs, check summaries, failure summaries, log references, and redacted log excerpts.
- Keep adapter telemetry hardening scoped and avoid presenting local/runtime-preview telemetry as production export.
- Keep policy-gated writes, generic runtime adapter execution, domain packs, production backends, hosted operation, distributed workers, OAuth, webhooks, and Level 3/4 autonomy deferred.

## 14. Final Recommendation

Proceed to maintainer-controlled release finalization.

Next steps:

1. Commit the release-pack changes and this final review.
2. Rotate the Jira sandbox API token or record the maintainer tracking item before public promotion.
3. Confirm `git status --short` is clean.
4. Create and push `v0.2.0-preview.1` using the release checklist.
5. Create the GitHub release from `docs/release/0.2.0-preview.1_GITHUB_RELEASE_DRAFT.md`.

This release should be announced only as:

**Workflow OS 0.2.0-preview.1 — Public Read-Only Integration Preview.**

It must not be announced as production-ready, write-capable, hosted, distributed-runtime-ready, broadly live-provider compatible, or Level 3/4 autonomy-ready.
