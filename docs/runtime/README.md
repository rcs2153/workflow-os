# Runtime

Runtime invariant documentation lives here.

The runtime has not been implemented yet. Future runtime documentation must cover immutable workflow definitions, content hashes, state transitions, event logs, idempotency, restart safety, and unsafe termination prevention.
