# Architecture

Architecture documentation lives here.

Current foundation:

- Rust owns the canonical core.
- TypeScript provides SDK ergonomics without defining a parallel model.
- v0 focuses on the local-first kernel.
- External integrations are deferred until core invariants are correct.
