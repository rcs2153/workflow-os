# Proportional-Governance Selected Local Project Consumer Plan Report

## 1. Executive Summary

Workflow OS now has a phase-ready plan for connecting the accepted
fresh-runtime-fact approval-to-authority-receipt-to-report-artifact closure to
one real local product path. The selected consumer is the existing
project-declared authoritative local project-validation path.

No runtime implementation was added.

## 2. Scope Completed

- Inspected the generic registered runtime-fact source consumer and complete
  approval-artifact composition.
- Inspected the existing Core-owned authoritative `DocsCheck` run, approval,
  report, and artifact path used by the CLI.
- Selected one closed product consumer and rejected generic default adoption.
- Defined Core-owned source and fact-ownership boundaries.
- Defined exact ordering, failure posture, compatibility, migration, and tests.
- Required an equivalence-first bridge before product-path adoption.

## 3. Scope Explicitly Not Completed

The phase added no Rust code, CLI behavior, executor default, automatic
approval, automatic persistence, provider or OpenShell integration, SideEffect
execution, external write, schema, example, hosted behavior, or release change.

## 4. Selected Consumer

The existing explicitly activated authoritative local project-validation path
is the first consumer because it already owns validated project activation, a
closed one-step profile, an immutable run bundle, canonical same-call
`DocsCheck` execution, proof-enforced approval, and a mandatory local WorkReport
artifact.

Ordinary executor calls and caller-authored runtime-fact sources are not
selected.

## 5. Trust Decision

Core must own the source registration and derive facts from frozen declarations,
the actual same-call check result, and current durable run context. The CLI may
request the selected operation but may not provide authority-bearing facts.
Unknown facts remain explicit and inference cannot weaken declared minima.

## 6. Migration Decision

Implementation starts with a Core-owned bridge and a test-only equivalence
matrix. An additive selected-consumer API follows only after review. CLI
adoption is a third, separately reviewed phase, and the existing path remains
available until semantic and failure-order equivalence is proven.

## 7. Validation

- `npm run check:docs`: passed.
- `git diff --check`: passed.
- Governed phase inspection and closure: passed.

## 8. Remaining Limitations

- No product path invokes the generic complete closure yet.
- The exact private fact-bound assessment adapter shape remains an
  implementation question.
- Multi-step authoritative governance and arbitrary local checks remain out of
  scope.
- Provider execution and external writes remain unauthorized.

## 9. Recommended Next Phase

Implement the Core-owned selected-profile fact-source bridge and test-only
equivalence matrix, then perform focused maintainer review before adding any
selected-consumer or CLI adoption behavior.

## 10. Governed Planning Record

- Dogfood workflow: `dg/d`
- Run ID: `run-1786438753010813000-2`
- Approval ID: `approval/run-1786438753010811000-2/planning-approved`
- Presentation ID: `presentation/85b1af1217db6a5a`
- Approval outcome: granted with persisted presentation proof
- Phase status: `Completed`
- Event summary: 39 events, 1 approval, 0 retries, 0 escalations
- Out-of-kernel work: architecture inspection and documentation authoring
