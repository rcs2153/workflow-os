schema_version: workflowos.dev/v0
id: approval/d
name: Self-Governance Approval Policy
description: Conservative local policy requiring explicit approval for scoped approval checkpoints.
rules:
  - id: require-human
    effect: require_approval
