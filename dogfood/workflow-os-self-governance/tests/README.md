# Self-Governance Dogfood Tests

No executable Workflow OS test specs are defined for the first dogfood slice.

This directory exists so the project layout is explicit. Future self-governed validation/check planning may add `.test.yml` files once test execution semantics are scoped.
