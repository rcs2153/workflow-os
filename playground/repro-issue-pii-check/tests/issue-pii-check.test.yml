schema_version: workflowos.dev/v0
id: repro/issue-pii-basic
name: Repro issue PII check validates and pauses for approval
target:
  id: repro/issue-pii
  version: v0
assertions:
  - id: requires-approval
    description: Workflow declares an approval-gated local step before recording attestation.
  - id: local-only
    description: Workflow uses a deterministic local mock skill and no external adapters.
