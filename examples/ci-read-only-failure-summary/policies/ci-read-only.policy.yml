schema_version: workflowos.dev/v0
id: approval/ci-read-only
name: CI Read-Only Fixture Approval Policy
description: Requires approval before final diagnosis output.
rules:
  - id: require-human-review
    effect: require_approval
