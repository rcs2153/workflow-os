schema_version: workflowos.dev/v0
id: approval/default
name: Default Local Approval Policy
description: Conservative local policy shell requiring approval before the deterministic mock skill runs.
rules:
  - id: require-human
    effect: require_approval
