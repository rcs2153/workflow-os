#![deny(unsafe_code)]
#![doc = "Command-line interface foundation for Workflow OS."]
#![doc = ""]
#![doc = "This binary intentionally exposes no supported workflow commands yet."]

fn main() {
    println!(
        "Workflow OS v0 foundation: local-first kernel work has not implemented workflow execution yet."
    );
}
